# CLAUDE.md — camber

This file is generated by `${workspace_root}/orbit/config/agent-doc-sync/master.json` via `orbit/scripts/agent-doc-sync.sh`.
Edit the master JSON / templates, not this file.

## Identity (SSOT)

- Contract (allowed lanes/seats/clients/platforms): `${workspace_root}/orbit/apps/mcp-server/config/session_identity.json`
- TRAM roles (fixed): `CHAD`, `STRAT`, `DEV`, `DATA`, `OPS`
- Session id format (seat-based): `<seat>-<client>-<n>`

## Launch (Claude Code)

Preferred: use the seat-based tmux launcher so env is set consistently.

1) Install tmux tools (once):
   - `cd ${workspace_root}/orbit/tools/tmux && bash install.sh`

2) Launch a seat:
   - `cmax <seat> [--role DEV|DATA|STRAT|OPS]`

Launchers export:
- `ORBIT_SESSION_ID`
- `ORBIT_ROLE`
- `ORBIT_ORIGIN_CLIENT`
- `ORBIT_ORIGIN_PLATFORM`

## Required boot (write-gated tools)

Before using any state-changing MCP tool, you must have a registered identity:

`session_register(session_id=$ORBIT_SESSION_ID, role=$ORBIT_ROLE, origin_client=$ORBIT_ORIGIN_CLIENT, origin_platform=$ORBIT_ORIGIN_PLATFORM, capabilities_version="v1")`

Boot activation (required before any non-test TRAM message):
- Receipt format: `subject=boot_activation__{origin_session}__{epoch}`
- `tram_create(to="STRAT", from=$ORBIT_ROLE, kind="status_update", subject=boot_activation__{origin_session}__{epoch}, origin_session=$ORBIT_SESSION_ID, origin_client=$ORBIT_ORIGIN_CLIENT, origin_platform=$ORBIT_ORIGIN_PLATFORM, ack_andon="yes", content="BOOT: SESSION=..., ROLE=..., COMMS=..., DECK=..., CAP_VER=v1")`
- Verify immediately: `tram_status(receipt=your_receipt)` (retry once with a new `{epoch}` if missing).

Write calls must carry `origin_session`, `origin_client`, `origin_platform` and a valid TRAM role field.

## Repo pointers

- Placement rules: `${workspace_root}/camber/MANIFEST.md`
- Repo overview: `${workspace_root}/camber/README.md`

