# AGENTS.md — camber-devfix-ios-inbox

This file is generated by `${workspace_root}/orbit/config/agent-doc-sync/master.json` via `orbit/scripts/agent-doc-sync.sh`.
Edit the master JSON / templates, not this file.

## Identity (SSOT)

- Contract (allowed lanes/seats/clients/platforms): `${workspace_root}/orbit/apps/mcp-server/config/session_identity.json`
- TRAM roles (fixed): `CHAD`, `STRAT`, `DEV`, `DATA`, `OPS`
- Session id format (seat-based): `<seat>-<client>-<n>`
  - `<seat>` is `<lane>` or `<lane>-<function>` (see contract)
  - `<client>` must match the `<client>` segment embedded in the session id

## Write gating (hard reject)

All state-changing MCP tools hard-reject unless the caller includes:
- `origin_session`, `origin_client`, `origin_platform`
- a valid TRAM role field (`from` / `reader` / `role` / `from_role` depending on tool)

and the session has an active presence row (`session_register` first).

Read-only tools remain open.

## Boot sequence (when user assigns a role)

When the user says “you are {role}”:

1) `session_register(session_id=SESSION, role=ROLE, origin_client=CLIENT, origin_platform=PLATFORM)`  
   Must return `{registered:true}` (or `{conflict:true,...}` if already active).

2) COMMS TEST: `tram_search(query="boot_activation", limit=1)`

3) BOOT ACTIVATION:
   - Use a unique receipt:
     - `subject=boot_activation__{origin_session}__{epoch}`
   - `tram_create(to="STRAT", from=ROLE, kind="status_update", subject=boot_activation__{origin_session}__{epoch}, origin_session=SESSION, origin_client=CLIENT, origin_platform=PLATFORM, ack_andon="yes", content="BOOT: SESSION=..., ROLE=..., COMMS=..., DECK=..., CAP_VER=v1")`

4) VERIFY: `tram_status(receipt=your_receipt)`  
   If missing → retry step 3 once with a new `{epoch}` → if still missing → report boot activation failure.

5) Load docs as needed (boot protocol, charter, role boundaries, contracts).

6) INBOX: `tram_my_queue(to=ROLE, origin_session=SESSION, include_content=false, limit=5)`

7) Claim top task or report “no work, awaiting directive.”

## Operator spawn policy (human workflow)

- Chad’s default fleet: spawn `STRAT`, `DEV`, `OPS`.
- Spawn `DATA` only when STRAT explicitly requests DB/migrations/data-integrity work.

## Repo pointers

- Optional local devfix workspace.
- Placement rules: `${workspace_root}/camber/MANIFEST.md`
- Repo overview: `${workspace_root}/camber/README.md`

